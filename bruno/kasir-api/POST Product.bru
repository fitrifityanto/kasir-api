meta {
  name: POST Product
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/product
  body: json
  auth: inherit
}

body:json {
  {"name": "Beras", "price": 70000, "stock": 10}
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: POST Category
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/categories
  body: json
  auth: inherit
}

body:json {
      {
        "name": "Minuman",
        "description": "Kategori produk minuman"
      }
}

settings {
  encodeUrl: true
  timeout: 0
}
